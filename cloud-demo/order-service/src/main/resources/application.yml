server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://47.103.198.68:3306/cloud_order?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: whyhdm
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        cluster-name: HZ # 集群名称
        # namespace: a4f5fe8c-2075-4674-9c70-2d4f9b3f5879 # 命名空间，填id
  application:
    name: orderservice
mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS

userservice:
  ribbon:
    NFLoadBalancerRuleClassName: com.alibaba.cloud.nacos.ribbon.NacosRule # 负载均衡规则
